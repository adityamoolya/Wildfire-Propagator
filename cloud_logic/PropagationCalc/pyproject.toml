[project]
name = "propagationcalc"
version = "0.1.0"
description = "Fire spread prediction and emergency call orchestration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.34.0",
    "httpx>=0.28.0",
    "python-dotenv>=1.1.0",
]
